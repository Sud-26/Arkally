import { AbstractControl } from '@angular/forms';
/**
 * Mark form control and all descendents as dirty and touched.
 *
 * @param control
 */
export declare const markAllAsDirtyAndTouched: (control: AbstractControl) => void;
