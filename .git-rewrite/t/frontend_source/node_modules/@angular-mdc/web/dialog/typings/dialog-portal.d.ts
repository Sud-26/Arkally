import { ComponentRef, EmbeddedViewRef } from '@angular/core';
import { Subject } from 'rxjs';
import { BasePortalOutlet, ComponentPortal, CdkPortalOutlet, TemplatePortal } from '@angular-mdc/web/portal';
import { MdcDialogConfig } from './dialog-config';
/**
 * Throws an exception for the case when a ComponentPortal is
 * attached to a DomPortalOutlet without an origin.
 */
export declare function throwMdcDialogContentAlreadyAttachedError(): void;
export declare class MdcDialogPortal extends BasePortalOutlet {
    _config: MdcDialogConfig;
    _portalOutlet: CdkPortalOutlet;
    /** A subject emitting after the dialog exits the view. */
    _afterExit: Subject<void>;
    /** ID for the container DOM element. */
    _id: string;
    /**
     * Attach a ComponentPortal as content to this dialog container.
     * @param portal Portal to be attached as the dialog content.
     */
    attachComponentPortal<T>(portal: ComponentPortal<T>): ComponentRef<T>;
    /**
     * Attach a TemplatePortal as content to this dialog container.
     * @param portal Portal to be attached as the dialog content.
     */
    attachTemplatePortal<C>(portal: TemplatePortal<C>): EmbeddedViewRef<C>;
    constructor(_config: MdcDialogConfig);
}
