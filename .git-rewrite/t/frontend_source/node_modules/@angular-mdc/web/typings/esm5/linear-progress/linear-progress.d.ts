import { ChangeDetectorRef, ElementRef, OnDestroy, OnInit } from '@angular/core';
export declare class MdcLinearProgress implements OnInit, OnDestroy {
    private _changeDetectorRef;
    elementRef: ElementRef<HTMLElement>;
    open: boolean;
    private _open;
    determinate: boolean;
    private _determinate;
    reversed: boolean;
    private _reversed;
    secondary: boolean;
    progress: number;
    private _progress;
    buffer: number;
    private _buffer;
    createAdapter(): {
        addClass: (className: string) => void;
        getPrimaryBar: () => Element | null;
        getBuffer: () => Element | null;
        hasClass: (className: string) => boolean;
        removeClass: (className: string) => void;
        setStyle: (el: HTMLElement, styleProperty: string, value: string) => void;
    };
    private _foundation;
    constructor(_changeDetectorRef: ChangeDetectorRef, elementRef: ElementRef<HTMLElement>);
    ngOnInit(): void;
    ngOnDestroy(): void;
    /** Retrieves the DOM element of the component host. */
    private _getHostElement;
}
