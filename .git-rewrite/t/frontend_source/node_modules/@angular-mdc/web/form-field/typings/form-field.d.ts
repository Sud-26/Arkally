import { AfterContentInit, ChangeDetectorRef, ElementRef, NgZone, OnDestroy, QueryList } from '@angular/core';
import { MdcFormFieldControl } from './form-field-control';
import { MdcHelperText } from './helper-text';
export declare class MdcFormField implements AfterContentInit, OnDestroy {
    private _changeDetectorRef;
    private _ngZone;
    elementRef: ElementRef<HTMLElement>;
    /** Emits whenever the component is destroyed. */
    private _destroy;
    label?: HTMLElement;
    fluid: boolean;
    private _fluid;
    alignEnd: boolean;
    private _alignEnd;
    _control: MdcFormFieldControl<any>;
    assistiveElements: QueryList<MdcHelperText>;
    constructor(_changeDetectorRef: ChangeDetectorRef, _ngZone: NgZone, elementRef: ElementRef<HTMLElement>);
    ngAfterContentInit(): void;
    ngOnDestroy(): void;
    private _initHelperTextFoundation;
    private _loadListeners;
}
